import { RecopiladoresShell } from "./RecopiladoresShell";

/** Route boundary only; all module behavior lives in owned section files. */
export default function RecopiladoresPage() {
  return <RecopiladoresShell />;
}
