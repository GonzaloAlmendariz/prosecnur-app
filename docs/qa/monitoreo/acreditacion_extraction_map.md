# Acreditacion extraction map

Source TSX: `frontend/src/features/monitoreo/MonitoreoPage.tsx`

Source CSS:
- `frontend/src/features/monitoreo/monitoreo.css`
- `frontend/src/features/monitoreo/shell/monitoreoShell.css`
- `frontend/src/features/monitoreo/salidas/outputsWorkbench.css`

Source backend:
- `api/R/router_monitoreo.R`
- `api/R/monitoreo_engine.R`

Target:
- `frontend/src/features/monitoreo/profiles/acreditacion/`

Initial evidence:
- `frontend/src/features/monitoreo/profiles/acreditacion/index.ts` imported `../../MonitoreoPage` before this loop.
- `AcreditacionMonitoreoPage.tsx` already existed but was not wired as the profile page.
- Backend exposes accreditation reports through `/api/monitoreo/state` scopes and mutating seguimiento/cierre through `/api/monitoreo/acreditacion/seguimiento` and `/api/monitoreo/cierre`.
- Canonical model source is `AcreditacionOperationalModel` in `MonitoreoPage.tsx`.
- Canonical seguimiento/cierre source is `AcreditacionPanel` in `MonitoreoPage.tsx`.
- Canonical local tabs detected for accreditation: `Fuentes` -> `survey/sheets/activas`; `Modelo` -> `estructura/casos/enlaces/reglas/estrategias`; `Consultas` -> `casos/efectivas/faltantes/duplicados/diferencias`; `Telefonico` -> `resumen/dia/responsables/alertas`; `Avance` -> `resumen/actores/encuestas/detalle/salidas`.

| Section | Tab/Subtab | Original TSX/CSS | Backend/API | Current target | Similarity | Cache key | Cold load risk | Warm cache behavior | Invalidates on | Extraction decision | Evidence |
|---|---|---|---|---|---:|---|---|---|---|---|---|
| Fuentes | Encuestas / Sheets / Fuentes activas | Generic source console, accreditation presets, `SourceSyncActions`, Google Sheets adjustment, SurveyMonkey catalog/inspection and source list classes `mon-source-*`; canonical local tabs `survey/sheets/activas`. | `/api/monitoreo/state?report_scope=source`, `/api/monitoreo/sheets/inspect`, `/api/monitoreo/sheets/source`, `/api/monitoreo/source`, `/api/monitoreo/sources`, `/api/monitoreo/sheets/sync`, `/api/monitoreo/sync`, SurveyMonkey multibase list/inspect endpoints, source metadata in state. | `AcreditacionSourcesWorkbench` now switches between survey sources, Sheets sources and active package evidence; the initial body opens with the canonical fixed-source architecture, requirement cards and SurveyMonkey summary, then exposes API-backed SurveyMonkey search/inspect/add, Google Sheets adjustment, configured-source label update and real sync actions. | 4.7 | `source` | Low for package/read-only rows; medium for search/inspect/sync jobs; high for real add/update/sync clicks without disposable fixture. | Uses warm cache via `apiMonitoreoState`; source add/update responses replace state; Sheets sync replaces state immediately; broader sync polls the local job then refreshes `source` scope. | Source add/update/sync. | Canonical package, picker, editor and sync controls are wired; safe mutating source click tests remain next loop on a disposable project. | Final direct QA on `ACRDCONTA.pulso` passed for `Encuestas`, `Sheets` and `Fuentes activas` at `tmp/visual-qa/monitoreo-acreditacion-acrdconta-fuentes-picker-iteration18-encuestas-closure/report.json`, `tmp/visual-qa/monitoreo-acreditacion-acrdconta-fuentes-picker-iteration18-sheets-closure/report.json` and `tmp/visual-qa/monitoreo-acreditacion-acrdconta-fuentes-picker-iteration18-activas-closure/report.json`; strict canonical comparator passed and was visually inspected at `tmp/visual-qa/monitoreo-acreditacion-canonical-compare-iteration18-final-strict/report.json`. |
| Modelo | Metas / Base / Enlaces / Reglas / Calendario | `AcreditacionOperationalModel`, `PlatformRejectionRulePanel`, actor meta cards, source mechanism map; canonical `OPERATIONAL_MODEL_MODES`. | `/api/monitoreo/config`, state variables/sources/goals, seguimiento/cierre endpoints. | `AcreditacionModelWorkbench` switches `estructura/casos/enlaces/reglas/estrategias`. When `acreditacion.enabled=false`, `AcreditacionModelConfigWorkbench` now renders a real config fallback with variables/metas, base de barrido, enlaces, estados/eventos/reglas and calendario/fases, and saves through `apiMonitoreoConfig`. Exact rejection-rule panel, collector inspector and full canonical model diagnostics remain partial. | 4.3 | `queries_summary` plus state config. | Medium: config save and exact rejection-rule/collector depth need disposable-fixture mutation tests. | Current page caches state per view; `apiMonitoreoConfig` response can replace state through `onStateChange`; non-mutating tab rendering uses warm `state.config`. | Config save, goal updates, rejection rules, seguimiento save, cierre. | Canonical tabs and disabled-state config fallback are wired; exact `AcreditacionOperationalModel` inspector depth remains next loop. | Click QA `Modelo` -> `Base de barrido` passed earlier; Iteration 19 direct QA passed for `Modelo` -> `Metas y modalidades` and `Modelo` -> `Estados válidos` at `tmp/visual-qa/monitoreo-acreditacion-acrdconta-modelo-iteration19-estructura/report.json` and `tmp/visual-qa/monitoreo-acreditacion-acrdconta-modelo-iteration19-reglas/report.json`; comparator route passed at `tmp/visual-qa/monitoreo-acreditacion-canonical-compare-iteration19-modelo/report.json`. |
| Modelo | Seguimiento/cierre | `AcreditacionPanel` with cards, n efectivo, intentos, notas, plan de refuerzo, aprobacion metodologica. | `/api/monitoreo/acreditacion/seguimiento`, `/api/monitoreo/cierre`. | `AcreditacionModelWorkbench` with real save/cierre handlers. | 4.0 | `queries_summary` | Low. | Warm cached state updated from mutation response. | Seguimiento save, cierre. | Extracted focused contract without backend changes. | Typecheck and focused tests passed after patch. |
| Consultas | Casos / Efectivas / Faltantes / Duplicados / Diferencias | `ConsultasView`, `INTERNAL_QUERY_MODES`, `InternalAssistedReviewBlock`, internal query helpers and reconciliation audit surfaces. | `/api/monitoreo/state?report_scope=queries_summary`, `/api/monitoreo/acreditacion/case-reconciliation`. | `AcreditacionConsultasPanel` uses `case_rollup` for ordinary case/advance tabs and full `internal_queries.cases` for `Duplicados`/`Diferencias`; assisted review controls are wired to the real case-reconciliation API with candidate selection, confirmation, note and status feedback; the body now uses the canonical case-explorer layout classes. | 4.6 | `queries_summary` | Medium-high cold path on ACRDCONTA (~30s for `queries_summary`), low once warm; local slices cap tables while prioritizing reviewable rows. | Warm cache reused by view loader; reconciliation mutation refreshes `queries_summary` with `force=true`. | Case reconciliation decisions, sync/report regeneration. | Canonical read tabs and safe mutation surface are wired; mutating buttons still need disposable-fixture click validation. | CSV `ACRDCONTA-alertas-appscript-vs-reconciliacion-app-20260623.csv` fields map to UI columns: counts, duplicates, partials, primary/secondary evidence and phone action. Click QA `Consultas` -> `Duplicados` passed; strict non-mutating QA `Consultas` -> `Diferencias` passed with `.mon-assisted-review` attached at `tmp/visual-qa/monitoreo-acreditacion-acrdconta-consultas-diferencias-assisted-polished`; final canonical explorer QA passed at `tmp/visual-qa/monitoreo-acreditacion-acrdconta-consultas-diferencias-final-fixed`. |
| Telefonico | Resumen / Día / Responsables / Alertas | Canonical `PhoneMonitoringPanel` local tabs and phone report helpers. | `monitoreo_telefonico` sheet blocks when available; `internal_queries` fallback under `queries_summary`. | `renderPhoneView` now switches all four canonical tabs and falls back to internal queries instead of blocking on `full`. | 4.0 | `queries_summary` | Medium-low: summary loads without full phone workbook; full report still useful for richer blocks. | Warm cache via current state loader. | Phone source sync/report regeneration. | Repaired scope/perf for initial tab use; richer phone-specific blocks remain future work. | Initial click `Telefono` -> `Responsables` failed under `full`; after switching to `queries_summary`, QA passed at 1440x900 with no errors/overflow. |
| Avance | Resumen | Canonical `AcreditacionAdvanceWorkbench` summary: `mon-advance-panel`, synchronized-cut hero, `AdvanceStorageSummary`, `AdvanceDailyMiniChart`, `AdvanceFocusList`, client report actors/daily and outputs. | `/api/monitoreo/state?report_scope=advance_summary`, `client_report.actors`, `client_report.daily_general`, `monitoreo_config.goals`. | `AcreditacionAdvanceSummaryWorkbench` now renders the canonical `mon-advance-*` structure with real actor cards, universe/progress bar, daily rhythm table and actor focus derived from `client_report` plus goals. Exact lazy Plotly chart remains represented by table/metrics rather than the original chart component. | 4.5 | `advance_summary` | Medium on ACRDCONTA: cold direct QA for Avance took near the 120s timeout; first render still shows scoped loading. | Warm cache by view; no mutating action in this slice. | Sync/publication. | Summary visual parity repaired for the worked slice; exact Plotly chart and remaining subtabs stay open. | Direct QA `Avance` -> `Resumen` passed at `tmp/visual-qa/monitoreo-acreditacion-acrdconta-avance-iteration20-resumen-fixed/report.json` after fixing date-header overflow. Comparator route passed at `tmp/visual-qa/monitoreo-acreditacion-canonical-compare-iteration20-avance/report.json`, but comparator readiness still anchors on Fuentes. |
| Avance | Actores | Canonical actor progress cards and client report actors. | `client_report.actors`. | Dedicated actor table and KPI row. | 4.0 | `advance_summary` | Low. | Local reductions. | Sync/publication. | Improved. | Typecheck passed. |
| Avance | Encuestas | Canonical sources/channel progress. | `client_report.sources`, sheet source blocks. | Dedicated source/channel table and KPIs. | 4.0 | `advance_summary` | Low. | Local reductions. | Sync/publication. | Improved. | Typecheck passed. |
| Avance | Detalle | Canonical `AcreditacionControlVariablesPanel`, `AcreditacionGsReportsPanel`, `mon-advance-detail-stack`, control variable grouping and `mon-gs-report-*` / `mon-report-cell-*` table grammar. | `/api/monitoreo/state?report_scope=advance_summary`, `reports.sheets`, `client_report.controls`, `client_report.actors`, `client_report.daily_general`, `client_report.sources`; `internal_queries` only if a richer scope is already present. | `AcreditacionAdvanceDetailWorkbench` now renders the canonical `mon-advance-panel` hero/KPI surface plus the detail stack. Control rows are grouped by variable/segment, report sheets prefer backend `reports.sheets`, and fallback sheets are derived from real `client_report` blocks instead of fabricated rows. Empty controls render as a compact real empty state. | 4.4 | `advance_summary` | Medium-high on ACRDCONTA cold loads: one prefetch attempt timed out before the final direct QA succeeded. | Warm cache reuses the loaded `advance_summary` state; no mutating action in this slice. | Sync/publication. | Detail visual parity repaired for the worked slice. Exact control rows depend on backend scope availability, and comparator deep-linking still needs a route/query affordance for subtab-specific side-by-side proof. | Direct QA `Avance` -> `Detalle` passed at `tmp/visual-qa/monitoreo-acreditacion-acrdconta-avance-iteration21-detalle-fixed-prefetch/report.json`; canonical comparator bootstrap passed at `tmp/visual-qa/monitoreo-acreditacion-canonical-compare-iteration21-detalle-bootstrap/report.json`, but readiness still anchors on Fuentes. |
| Avance | Salidas | Canonical `MonitoreoOutputsWorkbench`. | PDF/Sheets publication endpoints. | Reuses `MonitoreoOutputsWorkbench` for accreditation. | 4.2 | current state + publication metadata | Medium: publication actions can run jobs/network. | Refreshes current view after publication. | Publish/download actions. | Kept. | Existing handler preserved. |
