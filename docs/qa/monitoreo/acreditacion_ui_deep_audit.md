# Acreditacion UI deep audit

Tipo: Fuente histórica QA
Estado: Histórico
Fecha: 2026-06-30
Autoridad: Evidencia histórica fechada; no certifica el producto actual
Consolidado en: [Síntesis de Monitoreo de acreditación](../historico/monitoreo-acreditacion-2026-06.md)

## Scope lock - Iteration 24

- Module: `Monitoreo > Acreditacion > Modelo > Enlaces`.
- Product files intended: `frontend/src/features/monitoreo/profiles/acreditacion/AcreditacionMonitoreoPage.tsx`, `frontend/src/features/monitoreo/profiles/profilePage.css`.
- QA files intended: `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_channel_selector_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`.
- Explicit exclusions: `api/`, `frontend/src/features/monitoreo/MonitoreoPage.tsx`, `.pulso` project data, secrets, deprecated `../prosecnur/`, territorial/aulas profiles, destructive source syncs on `ACRDCONTA.pulso`.
- Main risk: adding a channel/collector classifier that looks complete but does not persist to `operational_model.link_collectors` or source dimensions.
- Minimum validation command: `pnpm --dir frontend typecheck`, `pnpm --dir frontend build:fast`, `git diff --check`, import audit for monolith coupling, and ACRDCONTA visual QA for `Modelo > Enlaces`.
- Category/source of truth: Prosecnur local-first desktop UI, `docs/adrs/0022-monitoreo-perfiles-frontend-dinamicos.md`, `docs/arquitectura-prosecnur.md`, the canonical collector contract already present in the monolith, and ACRDCONTA visual evidence.

## Scope lock - Iteration 25

- Module: `Monitoreo > Acreditacion > Telefono > Dia`.
- Product files intended: `frontend/src/features/monitoreo/profiles/acreditacion/AcreditacionMonitoreoPage.tsx`.
- QA files intended: `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`.
- Explicit exclusions: `api/`, `frontend/src/features/monitoreo/MonitoreoPage.tsx`, `.pulso` project data, secrets, deprecated `../prosecnur/`, territorial/aulas profiles, output workbench changes, source/model/case mutating clicks.
- Main risk: changing the chart grammar could hide the `Sin fecha` daily row and regress the already validated 14-row/145-response ACRDCONTA contract.
- Minimum validation command: baseline typecheck/diff/import audit before product edits; final typecheck, build:fast, Vitest, `git diff --check`, import audit, direct ACRDCONTA QA for `Telefono > Dia`, and comparator capture for `Telefono/Dia`.
- Category/source of truth: canonical phone trend chart grammar from `MonitoreoPage.tsx`, official date helpers in `internalQueries.ts`, raw `monitoreo_telefonico.avance_efectivo_dia` block in `ACRDCONTA.pulso`, Prosecnur local-first desktop UI, and ACRDCONTA visual evidence.

## Scope lock - Iteration 26

- Module: `Monitoreo > Acreditacion > Telefono > Dia` canonical comparator parity.
- Product files intended: `frontend/src/features/monitoreo/MonitoreoPage.tsx`, `scripts/monitoreo-visual-parity-check.mjs`.
- QA files intended: `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`.
- Explicit exclusions: `api/`, `.pulso` project data, secrets, deprecated `../prosecnur/`, territorial/aulas profiles, output workbench changes, source/model/case mutating clicks.
- Main risk: changing the canonical chart and comparator together could make a weak visual check pass; evidence must prove the raw `Sin fecha` row is preserved in both panes.
- Minimum validation command: baseline typecheck/diff/import audit; final typecheck, build:fast, `git diff --check`, direct ACRDCONTA QA and strict comparator capture for `Telefono/Dia`.
- Category/source of truth: raw `monitoreo_telefonico.avance_efectivo_dia` block in `ACRDCONTA.pulso`, canonical/independent side-by-side comparator evidence, Prosecnur local-first desktop UI, and ACRDCONTA visual evidence.

## Scope lock - Iteration 27

- Module: `Monitoreo > Acreditacion > Telefono > Alertas`.
- Product files intended: `frontend/src/features/monitoreo/profiles/acreditacion/AcreditacionPhoneAlerts.ts`, `frontend/src/features/monitoreo/profiles/acreditacion/AcreditacionMonitoreoPage.tsx`, `frontend/src/features/monitoreo/monitoreo.css`, `frontend/src/lib/SessionContext.tsx`, `frontend/src/app/SessionLostBanner.tsx`, `api/R/monitoreo_engine.R`, `api/R/router_monitoreo.R`, `api/tests/testthat/test-monitoreo-engine.R`.
- QA files intended: `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`.
- Explicit exclusions: `.pulso` project data, secrets, deprecated `../prosecnur/`, territorial/aulas profiles, Salidas, source/model/case mutating clicks and alert workflow mutations on the real ACRDCONTA project.
- Main risk: stale `phone_summary` or fallback-derived pending rows could keep inflating the independent alert count after the canonical alert sheet is available.
- Minimum validation command: `pnpm --dir frontend typecheck`, focused Monitoreo Vitest battery, R focal engine test, `git diff --check`, direct ACRDCONTA visual QA for `Telefono > Alertas`, and comparator attempt for the same slice.
- Category/source of truth: canonical `alertas/alertas` workbook block from `phone_summary`, ACRDCONTA visual evidence, Prosecnur local-first desktop UI, and the repair loop contract in `docs/loops-reparacion.md`.

## Scope lock - Iteration 28

- Module: `Monitoreo > Acreditacion > Telefono > Alertas` comparator legacy readiness.
- Product files intended: `frontend/src/features/monitoreo/MonitoreoPage.tsx`, `scripts/monitoreo-visual-parity-check.mjs`.
- QA files intended: `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`.
- Explicit exclusions: `.pulso` project data, secrets, deprecated `../prosecnur/`, Territorial/Aulas product logic, Salidas, source/model/case mutating clicks and alert workflow mutations on the real ACRDCONTA project.
- Main risk: the legacy frame could render the correct phone alert board briefly and then lose it when a later non-phone scope overwrites `dashboard.acreditacion_reports`.
- Minimum validation command: baseline/final `pnpm --dir frontend typecheck`, focused Monitoreo Vitest battery, `node --check scripts/monitoreo-visual-parity-check.mjs`, `git diff --check`, and strict ACRDCONTA comparator for `Telefono/Alertas`.
- Category/source of truth: `phone_summary` report coverage, canonical `alertas/alertas` workbook block, side-by-side comparator hydration blockers, ACRDCONTA visual evidence, Prosecnur local-first desktop UI, and the repair loop contract in `docs/loops-reparacion.md`.

## Scope lock - Iteration 29

- Module: `Monitoreo > Acreditacion > Consultas > Casos`.
- Product files intended: `frontend/src/features/monitoreo/profiles/acreditacion/AcreditacionMonitoreoPage.tsx`, `frontend/src/features/monitoreo/MonitoreoPage.tsx`.
- QA files intended: `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`.
- Explicit exclusions: `.pulso` project data, secrets, deprecated `../prosecnur/`, Territorial/Aulas product logic, Salidas, source/model/case mutating clicks and case reconciliation decisions on the real ACRDCONTA project.
- Main risk: improving the first viewport could hide case-level traceability or inflate source counts by counting report sheets instead of operational sources.
- Minimum validation command: baseline/final `pnpm --dir frontend typecheck`, focused Monitoreo Vitest battery, `git diff --check`, direct ACRDCONTA QA for `Consultas/Casos`, and strict ACRDCONTA comparator for `Consultas/Casos`.
- Category/source of truth: `queries_summary` official cases and audit cases, project `sources`, canonical case explorer distribution grammar, ACRDCONTA visual evidence, Prosecnur local-first desktop UI, and the repair loop contract in `docs/loops-reparacion.md`.

## Scope lock - Iteration 30

- Module: `Monitoreo > Acreditacion > Consultas > Efectivas/Faltantes`.
- Product files intended: `frontend/src/features/monitoreo/profiles/acreditacion/AcreditacionMonitoreoPage.tsx`, `frontend/src/features/monitoreo/monitoreo.css`.
- QA files intended: `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`.
- Explicit exclusions: `.pulso` project data, secrets, deprecated `../prosecnur/`, backend/API, Territorial/Aulas product logic, Salidas, source/model/case mutating clicks and case reconciliation decisions on the real ACRDCONTA project.
- Main risk: chart facets could look interactive but fail to filter, or `Faltantes` could continue inflating recovery flow with all pending universe cases.
- Minimum validation command: baseline/final `pnpm --dir frontend typecheck`, focused Monitoreo Vitest battery, `pnpm --dir frontend build:fast`, `git diff --check`, and direct ACRDCONTA QA for `Consultas/Efectivas` and `Consultas/Faltantes`.
- Category/source of truth: `queries_summary` official cases, `internal_queries.pending_exit`, Prosecnur query explorer grammar, ACRDCONTA visual evidence, local-first desktop UI, and the repair loop contract in `docs/loops-reparacion.md`.

## Scope lock - Iteration 31

- Module: `Monitoreo > Acreditacion > Consultas readiness/QA`.
- Product files intended: none.
- QA/harness files intended: `scripts/ui-quick-check.mjs`, `docs/qa/monitoreo/acreditacion_ui_deep_audit.md`, `docs/qa/monitoreo/acreditacion_ui_audit.md`, `docs/qa/monitoreo/acreditacion_parity_matrix.md`, `docs/qa/monitoreo/acreditacion_visual_parity_matrix.md`.
- Explicit exclusions: `.pulso` project data, secrets, deprecated `../prosecnur/`, backend/API, Territorial/Aulas product logic, Salidas, visual product UI changes, source/model/case mutating clicks and case reconciliation decisions on the real ACRDCONTA project.
- Main risk: hiding the real cold `queries_summary` slowness by making the QA harness too permissive, or declaring `Efectivas` fully captured from logs without a final same-session screenshot.
- Minimum validation command: `node --check scripts/ui-quick-check.mjs`, `pnpm --dir frontend typecheck`, `git diff --check`, and ACRDCONTA direct QA evidence for Consultas same-session prefetch behavior.
- Category/source of truth: same-session `ui-quick-check` session seeding, `queries_summary` cache logs, ACRDCONTA visual evidence, Prosecnur local-first desktop UI, and the repair loop contract in `docs/loops-reparacion.md`.

## Iteration log

| Iteration | Failure / bottleneck | Focused change | Evidence | Status |
| --- | --- | --- | --- | --- |
| 24 | `Modelo > Enlaces` in the independent Acreditacion profile listed saved collectors but did not expose the professional channel selector by survey and collector requested by the repair prompt. | Added a local-first channel/collector matrix: SurveyMonkey source channel editing, snapshot collector discovery, collector use/channel/modality controls, compact response/recipient/link metrics, suggestion application, source channel persistence through `apiMonitoreoSource`, and collector persistence through `apiMonitoreoCollectorsConfig`. | Baseline before product edits passed: `pnpm --dir frontend typecheck`, `pnpm --dir frontend build:fast`, `git diff --check`, and monolith import audit. Final validation passed: `pnpm --dir frontend typecheck`, `pnpm --dir frontend build:fast` with the known circular chunk warning, `pnpm --dir frontend test` (34 files / 220 tests), `git diff --check`, import audit, and loaded ACRDCONTA QA `tmp/visual-qa/monitoreo-acreditacion-channel-selector-iteration24-loaded-final/report.json` with 1 capture, 0 visual issues, 0 scroll jails, 0 overflow, 0 page/API/resource/project/wait issues. | Done for this slice |
| 25 | `Telefono > Dia` had the correct daily source block, but the independent profile still rendered the compact advance mini-table that produced the clipped `ECHA` header artifact and did not match the canonical phone trend chart grammar. | Added `AcreditacionPhoneDailyTrend` with the shared lazy `PlotlyChart`, canonical `mon-phone-trend` CSS, official date parsing/axis labels, stacked bars for effective/partial/rejection counts and an accumulated line. The local chart includes the `Sin fecha` point in the series to preserve the raw 14-row/145-response block. | Baseline before product edits passed: typecheck, `git diff --check` and import audit. Final validation passed: typecheck, build:fast with known circular chunk warning, Vitest 34 files / 221 tests, `git diff --check`, import audit, and direct ACRDCONTA QA `tmp/visual-qa/acreditacion-telefono-dia-phone-trend-14cuts-20260629/report.json` with 0 visual/overflow/page/API/resource/wait issues. Comparator capture `tmp/visual-qa/acreditacion-parity-telefono-dia-phone-trend-20260629/15-telefono-dia.png` shows the remaining data-verdict gap: original 144/13 excludes `Sin fecha`, independent 145/14 preserves the raw block. | Done for this slice; exact canonical data decision remains open |
| 26 | `Telefono > Dia` comparator parity still failed the data verdict because canonical `PhoneDailyTrend` excluded undated rows and the harness daily-series check was too narrow. | Updated canonical `PhoneDailyTrend` to plot/acumulate all points while keeping the last dated point for `Último corte`, expanded comparator daily hydration to accept `cortes diarios`, and aligned `registry.test.ts` with the current single-view `telefonico` profile contract. | Typecheck, build:fast and `git diff --check` passed. Direct ACRDCONTA QA passed: `tmp/visual-qa/acreditacion-telefono-dia-canonical-undated-keep-20260629/report.json`. Strict comparator passed with `ready=1`, both frames hydrated and both panes showing 145/14: `tmp/visual-qa/acreditacion-parity-telefono-dia-undated-canonical-20260629/report.json`, screenshot `15-telefono-dia.png`. Focused Monitoreo Vitest battery passed 35 files / 222 tests. | Done for this slice |
| 27 | `Telefono > Alertas` first viewport used the right supervision workflow but inflated independent counts from fallback phone pending rows instead of the canonical alert sheet, and a cold-cache run surfaced `SessionLostBanner` crashing under provider loss during Vite/HMR refresh. | Added `AcreditacionPhoneAlerts` to parse and group canonical alert rows, included the `Alertas` sheet in `phone_summary`, bumped the dashboard cache key, stacked the alert layout, and made `SessionLostBanner` tolerant of missing session context. | Direct ACRDCONTA QA passed: `tmp/visual-qa/acreditacion-telefono-alertas-alert-sheet-session-guard-20260629/report.json`, screenshot `quick-monitoreo-1600x1000-auto.png`, 0 visual/overflow/page/API/resource/wait issues and visible 48 active alerts / 2 focos. Validation passed: typecheck, focused Monitoreo Vitest 35 files / 225 tests, R engine test 1390 passes, and `git diff --check`. Comparator `tmp/visual-qa/acreditacion-parity-telefono-alertas-alert-sheet-20260629/report.json` captured the modular frame at 48/2 but timed out because the legacy frame had no phone report. | Done for independent data semantics; side-by-side comparator remains blocked by legacy frame readiness |
| 28 | `Telefono > Alertas` still failed as a side-by-side proof in cold/fresh runs because the legacy frame could load `phone_summary` and then be overwritten by later non-phone state responses, returning to `Sin monitoreo telefónico` while the independent frame stayed ready. | Treated `report_scope=phone_summary` as valid phone coverage in the legacy helper, requested `phone_summary` for phone view hydration, preserved an existing valid phone report while `activeView=telefonico` when incoming state lacks phone blocks, and made the comparator prefetch the target accreditation scope before opening the side-by-side page. | Validation passed: `pnpm --dir frontend typecheck`, focused Monitoreo Vitest 35 files / 225 tests, `node --check scripts/monitoreo-visual-parity-check.mjs`, `git diff --check`. Fresh ACRDCONTA comparator passed: `tmp/visual-qa/acreditacion-parity-telefono-alertas-preserve-fresh-20260629/report.json` with `ready=1`, two hydrated frames, no blockers/errors, `environment_issues=0`, `wait_ms=36616`; screenshot `17-telefono-alertas.png` shows both panes at 48 active alerts / 2 focos. | Done for focused legacy readiness/comparator closure; mutating alert workflow validation remains pending on a disposable fixture |
| 29 | `Consultas > Casos` in the independent profile opened directly into table/detail, while the canonical original first shows an executive distribution by actor and source/base. A cold direct QA also showed the legacy frame could remain in `Preparando consultas` after later scopes arrived. | Added an independent `AcreditacionDistributionView` with `mon-query-chart-card` actor/source/channel breakdowns before the cases table, changed the Consultas source KPI to use project sources before falling back to report sheets, and preserved a valid `queries_summary` in the legacy frame while `activeView=consultas` when incoming state lacks query blocks. | Baseline and final validation passed: `pnpm --dir frontend typecheck`, focused Monitoreo Vitest 35 files / 229 tests, and `git diff --check`. Direct ACRDCONTA QA produced a useful cold-performance failure (`tmp/visual-qa/acreditacion-consultas-casos-distribution-20260629/report.json`, wait selector misses after a 56s+ build). Strict comparator then passed: `tmp/visual-qa/acreditacion-parity-consultas-casos-distribution-preserve-20260629/report.json` with `ready=1`, both frames hydrated, no blockers/errors, `environment_issues=0`, `wait_ms=50304`; screenshot `09-consultas-casos.png` shows both panes with 519 cases, 12 sources and actor/source distribution cards. | Done for this slice; `Consultas/Efectivas`, `Consultas/Faltantes` and cold `queries_summary` performance remain open |
| 30 | `Consultas > Efectivas` still used only KPI strip plus table/detail instead of the canonical date/channel/collector chart first viewport, and `Consultas > Faltantes` treated all pending cases as flow rows instead of only real `pending_exit` recoveries. | Extended the independent case breakdown model to date and collector, added visible `Fecha` filtering plus filterable `Por fecha`, `Por canal` and `Por recopilador` cards for Efectivas, switched Faltantes to `internal_queries.pending_exit` / explicit `pending_exit` only, and compacted the no-flow empty state without rendering an empty inspector. | Baseline and final validation passed: `pnpm --dir frontend typecheck`, focused Monitoreo Vitest 35 files / 231 tests, `pnpm --dir frontend build:fast`, and `git diff --check`. Direct ACRDCONTA QA for Efectivas: `tmp/visual-qa/acreditacion-consultas-efectivas-iteration30-20260629/report.json`, screenshot shows chart facets and `Fecha` filter, 0 visual/overflow/page/API/resource issues. Direct ACRDCONTA QA for Faltantes: `tmp/visual-qa/acreditacion-consultas-faltantes-final-iteration30-20260629/report.json`, screenshot shows 156 pending, 0 pending-exit cases, compact `Sin flujo disponible`, local detail `0 filas`, 0 visual/overflow/page/API/resource issues. Both reports are `ok=false` only because an intermediate wait selector missed during cold `queries_summary`; post-click selectors matched. | Done for direct-route UI semantics; strict side-by-side comparator remains pending because the standalone comparator run was environmental (`frontend_unavailable`) and manual retained-session navigation stalled on cold Consultas hydration |
| 31 | `ui-quick-check` warmed `queries_summary` in one API session but still opened the page with `devPulso`, creating a second session and forcing a second cold build. The 12s prefetch budget was also too short for ACRDCONTA's cold `queries_summary`. | Raised prefetch budget for heavy Monitoreo scopes, kept click timeout configurable, and changed capture navigation to omit `devPulso` when a preloaded session exists so localStorage/session seeding remains the source of truth. | `node --check scripts/ui-quick-check.mjs` and `git diff --check` passed after the harness change. Same-session Faltantes QA passed: `tmp/visual-qa/acreditacion-consultas-faltantes-same-session-iteration31-20260629/report.json` has `ok=true`, 0 wait misses, URL `http://127.0.0.1:5175/monitoreo`, matching stack/result session, and screenshot `quick-monitoreo-1440x1000-portable.png`. API log proves cold build then cache reuse: `queries_summary build_ms=65176` followed by `dashboard=cache build_ms=0 total_ms=1953`. Efectivas prefetch-stable QA passed visually in `tmp/visual-qa/acreditacion-consultas-efectivas-prefetch-stable-iteration31-20260629/report.json`, but that earlier run used duplicate sessions; the same-session rerun confirmed cache reuse in logs (`queries_summary build_ms=61834`, then multiple `dashboard=cache build_ms=0` responses) but was cut before screenshot after the browser/harness stalled. | Done for same-session harness bug and Faltantes direct QA; Efectivas same-session screenshot and strict side-by-side comparator remain pending; product cold build time remains slow |
| 32 | `Telefono > Resumen` / perfil `telefonico` made phone quotas look like a mandatory variable-first block, was not collapsible, and could show technical labels such as `dim_actor`. The top phone status legend also depended only on the explicit `estatus_telefonico` sheet, so it could look empty even when `internal_queries` had phone states. | Made `AcreditacionPhoneQuotaPanel` optional/collapsible with a persistent summary, grouped detail by actor first, added an all-variable filter plus per-row variable chips, normalized public-target sheet columns (`Actor especifico`, `Variable de cuota`, `Poblacion objetivo`) and humanized technical quota labels. `AcreditacionClarityStrip` now falls back to grouped phone case states when the explicit phone-status sheet is missing, while the phone legend remains gated to `activeView === "telefonico"`. | Validation passed: `pnpm --dir frontend exec vitest run src/features/monitoreo/profiles/acreditacion/AcreditacionMonitoreoPage.test.ts` (43 tests), `pnpm --dir frontend typecheck`, and `git diff --check`. Direct `telefonico_cuotas` QA passed collapsed at 1440x900 and 1024x640: `tmp/visual-qa/quick/2026-06-30T11-42-34-273Z/report.json`; expanded detail passed at both sizes: `tmp/visual-qa/quick/2026-06-30T11-42-56-761Z/report.json`; final full-page expanded proof passed and shows human labels `Actor`, `Distrito`, `Grupo`: `tmp/visual-qa/quick/2026-06-30T11-46-53-042Z/report.json`. | Done for the quota optionality/collapse/label slice; exact ACRDCONTA side-by-side scoring can be run later if parity scoring is needed |
