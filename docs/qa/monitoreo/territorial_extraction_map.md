# Territorial canonical extraction map

Tipo: Fuente histórica QA
Estado: Histórico
Fecha: 2026-06-27
Autoridad: Evidencia histórica fechada; no certifica el producto actual
Consolidado en: [Síntesis de Monitoreo territorial](../historico/monitoreo-territorial-2026-06.md)

This map was created before code changes in the canonical extraction loop. The source of truth is `frontend/src/features/monitoreo/MonitoreoPage.tsx`; `frontend/src/features/monitoreo/profiles/territorial/TerritorialMonitoreoPage.tsx` is treated as an incomplete target until each row has concrete target evidence.

## Baseline evidence

- Registry/cache tests: `pnpm --dir frontend test -- src/features/monitoreo/profiles/registry.test.ts src/features/monitoreo/core/reportScopeCache.test.ts` passed.
- Typecheck: `pnpm --dir frontend typecheck` passed.
- Fast build: `pnpm --dir frontend build:fast` passed.
- Diff hygiene: `git diff --check` passed.
- Import boundary: `frontend/src/features/monitoreo/profiles/territorial/index.ts` loads `./TerritorialMonitoreoPage` and does not import `../../MonitoreoPage`.
- Visual baseline: `scripts/ui-quick-check.mjs` captured `/monitoreo` with the territorial audit project at `1440x1000` and `1280x800`, with no reported layout issues.

## Inventory counts

- Original source blocks mapped: 42.
- Constants/types/config blocks mapped: 8.
- State and handler groups mapped: 10.
- Render blocks mapped: 7.
- Rich component groups mapped: 10.
- Helper/model groups mapped: 7.
- CSS class groups mapped: 8.
- Target rows with complete behavior parity before this loop: 10.
- Target rows extracted in this loop: 10 rich units (`avance/salidas`, `avance/resumen+ump+ritmo workbench`, `avance/ump interactive route-map repair`, `avance/ump lazy GPS layer + selected-manzana point focus`, `avance/ump street/context/zone/neighbor layer repair`, `avance/ritmo canonical daily v24`, `fuentes/source console`, `fuentes/batch reconciliation + sync progress`, `modelo/resumen + tabla workbench`, `modelo/coverage atlas map`), 5 quality units (`calidad/geolocalizacion` GPS/cartography scaffold, `calidad/reconciliacion` spatial panel with confirmation/focus handoff, `calidad/duracion` duration workbench, `calidad/cuotas` quota consistency panel, and `calidad/anulacion` production-annulment workspace), 2 consultas units (`TerritorialReviewCasesWorkbench` table/filter/action scaffold and `TerritorialOperationalAdjustmentsWorkspace` operational package workflow), 5 occurrence units (`TerritorialFieldOccurrencesWorkbench` states/UMP/observations, Kobo asset selector/config persistence, UMP district/responsible/outcome filters, upload/config/log command state, and searchable alert review board), plus 4 cache/status units (client warm-cache resolved value reuse with territorial initial-scope duplicate request guard, canonical global loading/error state with failed-report retry guard, rail phase/source/report-cache status backed by `territorial_phase_coherence` and `territorial_report_cache`, and `advance_summary` v24 canonical daily scoping for `avance/ritmo`). Iteration 34 adds a Fuente/Formulario visual parity repair on top of the already-extracted source console; Iteration 35 closes `Avance/Ritmo diario`; Iteration 36 restores visible `Avance/Mapa y UMP` context layers and GPS legend parity without changing backend scope; Iteration 37 restores a paneable, auto-focused `Calidad/Geolocalización` camera with selected-UMP GPS filtering and visible GPS-state legend; Iteration 38 adds active-district manzana context and the original selected-manzana versus nested-GPS accordion hierarchy; Iteration 39 closes the scripted proof for the post-accordion GPS point click; Iteration 40 restores selected-district street/context layers in the validation map; Iteration 41 restores the virtualized UMP group rail and nested GPS point focus; Iteration 42 restores district/section headings over the virtualized geolocation case rail; Iteration 43 corrects the sidebar containment semantics to the original published zone/district GPS criterion; Iteration 44 restores the richer original per-point detail summaries and GPS trace fields inside the opened UMP accordion; Iteration 45 restores the original case-first section split for mixed GPS/no-GPS groups.

## Extraction map

| Original area | Original symbol/block | Original source line/range | Role | Current target equivalent | Status | Extraction decision |
|---|---|---:|---|---|---|---|
| Imports | `MonitoreoOutputsWorkbench` | `MonitoreoPage.tsx:12` | Canonical outputs workbench used by the territorial `salidas` subtab. | `TerritorialOutputsPanel` wraps the canonical workbench for both the monolith and the territorial route. | extracted | Completed as the loop's bounded rich unit. |
| Imports | Territorial API client group | `MonitoreoPage.tsx:68-92` | Config, source, phase, map, Kobo inspection, sync, occurrences, and export actions. | `TerritorialSourceConsole` now uses source/config/Kobo/Sheets/roster API handlers directly; map/occurrences remain outside Fuentes. | partial | Source-facing handlers extracted without touching backend or connectors. |
| Imports | Territorial API types | `MonitoreoPage.tsx:151-176` | Strong typing for canonical territorial payloads and interactions. | Partial target typing for dashboard/state only. | mismatch | Reuse types as each action unit is extracted. |
| Imports | `territorialSummaryModel` | `MonitoreoPage.tsx:220-226` | Canonical aggregation model for territorial summaries. | No equivalent rich model layer in target. | mismatch | Blocked until the associated view is extracted. |
| Imports | `territorialGeoAssignment` | `MonitoreoPage.tsx:228-230` | Canonical geo assignment model. | No equivalent map/assignment model in target. | mismatch | Keep trapped in monolith for now. |
| Imports | `territorialCaseAuditModel` | `MonitoreoPage.tsx:232-238` | Canonical validation/review audit model. | No equivalent case-audit model in target. | mismatch | Blocked by validation/review extraction size. |
| Imports | `territorialMasterRecords` | `MonitoreoPage.tsx:240-244` | Canonical master-record model. | No equivalent in target. | mismatch | Keep mapped, not migrated. |
| Constants | `territorialRouteSheetDraftDefaults` | `MonitoreoPage.tsx:711` | Route-sheet defaults for the source/model workflow. | No equivalent editable route-sheet draft. | mismatch | Do not extract until source/model action surface is selected. |
| Constants | `TERRITORIAL_WORKBENCH_VIEWS` | `MonitoreoPage.tsx:942-948` | Canonical six main territorial sections. | Same six main sections in target. | done | Preserve; no code change required. |
| Constants | `TERRITORIAL_BOOT_STEP_DEFS` | `MonitoreoPage.tsx:959-1011` | Canonical boot/readiness sequence. | No full boot panel equivalent. | mismatch | Keep mapped as a future rich unit. |
| Constants | `localTabsForWorkbenchView()` | `MonitoreoPage.tsx:1021-1062` | Canonical local subtabs: fuentes 5, modelo 2, calidad 5, consultas 5, avance 4, ocurrencias 3. | Target has tab labels/order after phase 2, but most tabs share simplified content. | mismatch | Navigation is present; behavior parity is not complete. |
| Events | `emitMonitoreoLocalTabActive()` | `MonitoreoPage.tsx:1124` | Audit/local tab event emission. | Target uses profile-local state only. | mismatch | Defer until active-tab analytics/audit behavior is selected. |
| Helpers | `normalizeTerritorialPhase()` and phase-source helpers | `MonitoreoPage.tsx:1641-2023` | Canonical phase/source normalization and coherence state. | Target uses simplified phase cards from state. | mismatch | Keep trapped; too broad for this loop. |
| Helpers | Status/label helpers | `MonitoreoPage.tsx:2030-2114` | Canonical source, phase, and quality labels. | Target now reuses equivalent phase status and badge label logic for the modular rail, backed by `territorial_phase_coherence`. | partial | Iteration 29 extracts the rail-facing status labels; broader helper parity remains tied to full boot/readiness and rich validation/review surfaces. |
| Helpers | `mergeTerritorialConfig()` | `MonitoreoPage.tsx:2212` | Canonical config merge behavior. | No target equivalent. | mismatch | Do not move without config mutation handlers. |
| State | Selected phase/loading/view state | `MonitoreoPage.tsx:2909-2957` | Canonical state for phase routing, view loading, selected case, validation intent, review filters, and refs. | Target has simpler page/view state. | mismatch | Blocked for full parity; requires controlled state extraction. |
| Handler | `setAuthoritativeRoutePhase()` | `MonitoreoPage.tsx:2968` | Canonical authoritative phase switch. | Target has local phase selection only. | mismatch | Future model/source extraction. |
| Handler | `openTerritorialValidationCase()` | `MonitoreoPage.tsx:2974` | Cross-section case deep-link to validation. | Target now has a bounded `consultas` -> `calidad/geolocalizacion` or `calidad/duracion` deep link preserving `response_id`. | partial | Data-rich selected-case verification remains open. |
| Handler | Report-scope memory/reset | `MonitoreoPage.tsx:3002-3016` | Canonical report-scope preservation. | Target relies on shell/profile default state. | unverified | Leave unchanged until report behavior is audited. |
| Handler | Hydration/prewarm/boot logic | `MonitoreoPage.tsx:3093-3176` | Canonical warm-state readiness behavior. | Target has direct light-state fetch plus client warm-cache reuse for repeated resolved `warmupCache` calls, an initial-scope guard that prevents a duplicate view-scope request while the first scope hydrates, route-first `avance/ump` scope selection, reactive pending-scope tracking, background prefetch separated from active chrome busy state, failed-report guards that pause active/background report hydration until manual retry, a v23 route report-cache contract that strips Kobo schema field/choice lists, duplicate map/progress branches, and source-console-only reconciliation summaries, and a v24 `advance_summary` contract that lets `avance/ritmo` use the canonical audit daily rows while still scoping out `response_audit` and map points. | partial | Request hygiene improved in Iteration 18, first-render scope improved in Iteration 19, active loading feedback improved in Iteration 20, source-schema payload trimming landed in Iteration 22, route payload dedupe landed in Iteration 25, error retry guard landed in Iteration 28, and canonical daily rhythm scoping landed in Iteration 35. `route_summary` is smaller and the desktop map passes 1280-1920 px QA; `advance_summary` rhythm parity is closed visually/functionally, but cold rebuild remains slow because backend computation dominates the ACNURCG path. |
| Handler | Map preparation | `MonitoreoPage.tsx:3712` | Canonical route-map prefetch/prepare behavior. | No map-equivalent target workflow. | missing | Keep trapped in monolith. |
| Handler | Kobo inspection/source inspection | `MonitoreoPage.tsx:3814, 4883` | Critical source verification action. | `TerritorialSourceConsole` can list Kobo assets, apply a phase source, inspect Kobo, and start response sync using existing API handlers. | extracted | Mutating actions are wired; visual QA verifies tab surfaces, not external mutation. |
| Handler | `patchTerritorialConfig()` | `MonitoreoPage.tsx:3872` | Canonical config mutation path. | `TerritorialSourceConsole` saves phase mappings and active source config through `apiMonitoreoTerritorialConfig`. | extracted | Source configuration boundary moved into territorial profile. |
| Handler | Upload/reconciliation handlers | `MonitoreoPage.tsx:3915-3998` | Critical route-sheet and UMP reconciliation actions. | Roster upload, template/codes download, route-sheet connect/sync, selectable recommendations, and batch code/UMP apply are implemented. | extracted | Existing endpoint reused; no backend or connector behavior changed. |
| Handler | Operational adjustment handlers | `MonitoreoPage.tsx:4012-4058` | Critical review/adjustment behavior. | Target wires apply/revert/reset from `consultas/subsanaciones` to the existing operational-adjustment APIs and updates territorial state after each response. | partial | Handler parity is extracted; data-rich suggestion/apply/revert visual behavior remains unverified with the current fixture. |
| Handler | Annulment handlers | `MonitoreoPage.tsx:4073-4115` | Critical quality/annulment behavior. | `TerritorialProductionAnnulmentWorkspace` calls the existing preview/apply/revert endpoints and updates territorial state after mutations. | partial | Handler wiring is type/build validated; data-rich preview/apply/revert UI needs fixture verification. |
| Handler | Spatial dismissal handlers | `MonitoreoPage.tsx:4135-4156` | Critical geo-review behavior. | `TerritorialSpatialReconciliationWorkbench` calls the existing candidate/pattern dismiss endpoints from the territorial profile. | partial | Endpoint wiring is restored; candidate-rich visual/state parity still needs fixture verification. |
| Handler | Enumerator downloads | `MonitoreoPage.tsx:4172-4186` | Critical export/download action. | `TerritorialSourceConsole` downloads enumerator template and codes through existing API helpers. | extracted | Download handlers restored for Fuentes. |
| Handler | `syncExternalSources()` | `MonitoreoPage.tsx:4230` | External source sync orchestration. | `TerritorialSourceConsole` starts Kobo sync jobs for the active territorial source and polls job status for progress/result refresh. | extracted | Restores the source console progress state without changing connector logic. |
| Handler | Occurrences config/inspect/sync/upload handlers | `MonitoreoPage.tsx:4280-4330`; `router_monitoreo.R:5316-5510` | Critical field occurrence workflow plus existing backend XLSForm import/deploy endpoint. | `TerritorialFieldOccurrencesWorkbench.tsx` now wires Kobo account discovery, asset catalog loading, asset selection/config persistence, XLSForm generation, upload-Kobo, field inspection, occurrence sync, download/open URL refresh, state refresh, field-check display, and link copy through existing occurrence/source APIs. | partial | Upload-Kobo is visible and type/build validated, but the external mutation was not clicked in ACNURCG because the session has no safe tokened fixture; selector/config UI is visually verified. |
| Render | `TerritorialBootPanel` | `MonitoreoPage.tsx:4669` | Canonical boot/readiness UI. | No full target equivalent; the rail now shows the key source/coherence/cache facts while the boot panel itself remains unextracted. | mismatch | Iteration 29 improves the visible rail status only; future status/readiness extraction must cover the boot-step state machine. |
| Render | `TerritorialAdvanceView` | `MonitoreoPage.tsx:4708` | Canonical avance rich view and local subtabs. | `TerritorialAdvanceWorkbench.tsx` covers `resumen`, `ump`, and `ritmo`; `TerritorialOutputsPanel` covers `salidas`; `avance/ritmo` now uses canonical top-level `daily` rows from scoped `advance_summary` v24; `avance/ump` has a real cartography-backed UMP/manzana map plus lazy GPS layer loading, selected-manzana point filtering, response list, click-to-focus behavior, audited selected manzana/GPS inspector, route-first first render, opt-in street/context cartography, zone overlays, neighbor-block context, and the canonical GPS state legend. | partial | Rhythm visual/backend parity is closed in Iteration 35 with ACNURCG evidence. Iteration 36 verifies `Avance > Mapa y UMP` with 150 manzanas, 9 GPS points in map, 1,513 visible GPS, 90 zones, 220 streets, 34 context features, and 180 neighbor blocks. Full original map-engine parity and acceptable cold/cache timing remain open. |
| Render | `TerritorialQueriesView` | `MonitoreoPage.tsx:4750` | Canonical consultas workflow. | `TerritorialReviewCasesWorkbench` now covers the review hero, filterbar, canonical table shell, local tab filtering, UUID copy, and GPS/duration validation handoffs; `TerritorialOperationalAdjustmentsWorkspace` covers `subsanaciones` package selection and mutations. | partial | Data-rich audit drawer behavior and review-row verification remain open. |
| Render | `TerritorialFieldOccurrencesSection` | `MonitoreoPage.tsx:4799` | Canonical occurrence workflow. | `TerritorialFieldOccurrencesWorkbench.tsx` replaces the simplified target for `ocurrencias` and renders states, UMP follow-up, observations, command metrics, field-check state, Kobo asset selector/config state, upload-Kobo command state, config summary, searchable alert rows, richer history entries, and real occurrence summaries. | partial | The visible tabs, selector panel, UMP filters, alert board, upload button, config summary, and log entries are visually verified; external upload mutation remains unverified. |
| Render | `TerritorialRouteView` | `MonitoreoPage.tsx:4817` | Canonical modelo/route workflow. | `TerritorialModelWorkbench` renders route command state, route-sheet strip, sync handler, canonical coverage atlas/map, titular/replacement table and selected UMP context. | extracted | Modelo tabs are closed as rendered in the original route view; broader territorial GPS/cartography surfaces remain under Calidad/Avance. |
| Render | `TerritorialSourceConsole` | `MonitoreoPage.tsx:4860` | Canonical fuentes workflow. | `TerritorialSourceConsole.tsx` replaces the simplified `SourceView` path for `fuentes`. | extracted | Fuentes now has separate source console in territorial profile. |
| Render | `TerritorialValidationView` | `MonitoreoPage.tsx:4953` | Canonical calidad workflow. | `TerritorialValidationGeoWorkbench.tsx` covers `geolocalizacion` and `reconciliacion`; `TerritorialDurationControl.tsx` covers duration; `TerritorialQuotaConsistencyPanel.tsx` covers quotas; `TerritorialProductionAnnulmentWorkspace.tsx` covers annulment scaffolding/actions. Full action state remains incomplete. | partial | Continue Calidad before marking parity. |
| Component | `TerritorialLoadingView` | `MonitoreoPage.tsx:2740` | Canonical territorial loading state. | Target now renders the canonical `mon-territorial-loading-*` state for active report-dependent tabs while their scope is pending. | extracted | Iteration 20 restores canonical loading/feedback; paired with the Iteration 28 error-state repair, the global loading/error row is closed. |
| Component | `TerritorialViewError` | `MonitoreoPage.tsx:2816` | Canonical territorial error state. | Target now renders `TerritorialViewError` through shared `Alert kind="error"`, `mon-territorial-view-error`, and a real retry button. | extracted | Iteration 28 closes the status extraction and verifies that failed reports do not loop background requests before manual retry. |
| Component | `TerritorialAdvanceView` internals | `MonitoreoPage.tsx:8619-10820` | Rich executive summary, maps, district scorecards, UMP, rhythm, date filters, and outputs. | `TerritorialAdvanceWorkbench.tsx` renders the datebar, overview hero, executive dashboard, district cards, UMP table/detail, daily rhythm chart, outputs handoff, and an interactive UMP/manzana map with lazily loaded GPS points plus a canonical-style audited inspector. For `avance/ump`, the page accepts `route_summary` as the first report scope, defers `advance_summary` hydration for `avance/resumen`, and now loads rich street/context layers only for the selected UMP district; for `avance/ritmo`, it consumes canonical top-level `reports.daily` before `advance.daily`, matching the monolith. | partial | The target now supports the canonical rhythm chart/table with 13 ACNURCG days, pan, zoom, reset, selected feature focus, UMP navigator, selected-manzana GPS filtering, stable point scaling, response list, GPS point focus, audited block/GPS details, zone/context/street/neighbor layers, and a visible GPS-state legend. It avoids cold `advance_summary` in the measured Mapa y UMP path, but `ritmo` still pays the cold canonical audit cost and the full historical map engine/performance contract remains open. |
| Component | `MonitoreoOutputsWorkbench` inside territorial avance | `MonitoreoPage.tsx:8702` | Rich output panel with publication state/actions. | `TerritorialOutputsPanel` is consumed by `MonitoreoPage.tsx` and `TerritorialMonitoreoPage.tsx`. | extracted | Shared territorial outputs panel completed. |
| Component | Source console internals | `MonitoreoPage.tsx:10769-14957` | Readiness, route sheets, source config, reconciliation, roster. | Implemented as territorial profile component with five real subtabs: form, filter, roster, reconciliation, history, batch apply, and job progress state. | extracted | Fuentes is closed as a bounded canonical unit; advanced state outside Fuentes remains mapped separately. |
| Component | Route/model internals | `MonitoreoPage.tsx:14963-16800` | Route model, sheets, cartography, assignment. | `TerritorialModelWorkbench.tsx` covers route sheet state, canonical coverage atlas/map, assignment progress, titular/replacement table, UMP selection and context. | extracted | Modelo is no longer blocked on route-map parity; GPS/georeference review remains mapped under validation and advance map surfaces. |
| Component | Validation internals | `MonitoreoPage.tsx:16997-20883` | Geo, duration, quota, annulment, and quality audit workflows. | `calidad/geolocalizacion` now uses canonical classes, Hojas de Ruta cartography, Kobo GPS points, map footer metrics, and grouped UMP cases; `calidad/reconciliacion` now uses the canonical spatial panel and confirmation-dialog classes with queue/apply/dismiss endpoints; `calidad/duracion` now uses the canonical duration workbench; `calidad/cuotas` now uses the canonical quota consistency panel; `calidad/anulacion` now uses the production-annulment workspace. Exact spatial map focus and data-rich duration/reconciliation/annulment parity are still missing. | partial | Keep looping inside Calidad; do not migrate another family. |
| Component | Queries internals | `MonitoreoPage.tsx:20908-22854` | Internal queries, cases, adjustment history, audit filters. | `TerritorialReviewCasesWorkbench.tsx` extracts the canonical review-cases table/filter/action scaffold; `TerritorialOperationalAdjustmentsWorkspace.tsx` extracts the `subsanaciones` package picker, note, apply/revert/reset, empty state, metrics, and applied history. | partial | Audit drawer, batch UMP audit, and data-rich operational package verification remain open. |
| Component | Occurrence internals | `MonitoreoPage.tsx:22917-24209` | Field occurrence config, XLSForm, inspection, sync, UMP follow-up, state composition, alerts, and logs. | `TerritorialFieldOccurrencesWorkbench.tsx` renders state composition, outcome/daily charts, district matrix, Kobo selector, UMP search/status/district/responsible/outcome filters, UMP list/detail drawer, observation coverage cards, searchable alert rows, and a fuller history list. | partial | UMP filters, selector behavior, and the `Observaciones` alert board are visually verified on ACNURCG; upload-Kobo and mutating round-trip evidence remain pending. |
| Component | Map internals | `MonitoreoPage.tsx:24210-27604` | Canonical territorial route/assignment map surface. | `TerritorialValidationGeoWorkbench.tsx` adds a real territorial map using local Hojas de Ruta block/zone cartography plus Kobo GPS points, but it is not yet the exact historical map engine. | partial | Next repair must close map parity or explicitly block with remaining deltas. |
| Helper | Shared territorial tables/helpers | `MonitoreoPage.tsx:27617-27839` | Canonical tables and formatting shared by rich views. | Target has separate simplified tables. | mismatch | Extract with each owning rich unit. |
| Helper | `formatPercentLabel()` / `formatMetric()` | `MonitoreoPage.tsx:33511, 34050` | Canonical numeric formatting. | Target has local formatting helpers. | mismatch | Future shared formatting extraction. |
| CSS | Output suite classes | `monitoreo.css:1924-3268` | Canonical output workbench visual language. | Target uses output workbench through `TerritorialOutputsPanel` for `avance/salidas`. | extracted | Preserved by shared outputs panel. |
| CSS | Territorial date/executive/map classes | `monitoreo.css:5329-7388` | Canonical avance visual language. | Target now reuses `mon-territorial-datebar`, `mon-territorial-overview-*`, `mon-territorial-exec-*`, `mon-territorial-route-map-*`, `mon-territorial-map-*`, and `mon-territorial-inspector-card` classes in `TerritorialAdvanceWorkbench`; Iteration 36 adds a scoped `.mon-territorial-advance-map-legend` overlay using the canonical map legend chips. | partial | Desktop map language, selected UMP/manzana inspection, selected GPS inspection, GPS color legend, and context/street/zone/neighbor layer visibility are restored. Performance parity and the exact historical map engine remain incomplete. |
| CSS | Territorial UMP/rhythm classes | `monitoreo.css:7742+` | Canonical UMP/rhythm visual language. | Target now reuses `mon-territorial-ump-*` table/detail/response classes, `mon-territorial-rhythm-*` chart classes, and local `mon-territorial-ump-map-*` adaptations. | partial | `avance/ritmo` visual parity is verified with the Plotly chart/table in Iteration 35. Iteration 36 verifies the UMP map with visible GPS-state legend, 90 zone overlays, 220 street features, 34 context features, and 180 neighbor blocks in ACNURCG; remaining parity is map-engine/performance depth, not a missing visual layer. |
| CSS | Source console classes | `monitoreo.css:44757, 50620, 51342, 51963` | Canonical source console visual language. | `TerritorialSourceConsole` uses `mon-territorial-source-*`, route-sheet, history, and output-independent canonical classes. | extracted | Preserves historical source visual language; no new chrome namespace. |
| CSS | Route sheet/source classes | `monitoreo.css:45907+` | Canonical route/model visual language. | Modelo now reuses `mon-territorial-route-*`, `mon-territorial-route-sheet-*`, and `mon-territorial-route-coverage-*` classes; local CSS only adapts independent-page height/scroll behavior. | extracted | Preserve canonical visual language for route/model and coverage atlas. |
| CSS | Loading/error classes | `monitoreo.css:52480, 52896-53176` | Canonical status visual language. | Target uses canonical `mon-territorial-loading-*`, `mon-territorial-view-error`, and shared `pulso-alert-error` classes for active loading and error states. | extracted | Iteration 28 removes the old `ter-inline-error` target fallback and closes the global status visual language row. |
| CSS | Review/validation classes | `monitoreo.css:53199-53349` | Canonical quality/review visual language. | Calidad and Consultas now reuse `mon-territorial-validation-*`, `mon-territorial-review-*`, `mon-duration-*`, `mon-territorial-quota-*`, and `mon-production-annulments*` class families in the migrated profile. | partial | Remaining parity is behavior/data-rich verification, not a separate visual family. |
| CSS | Workbench chrome classes | `monitoreo.css`, `MonitoreoWorkbenchChrome` | Canonical shell/workbench layout language. | Target uses `MonitoreoWorkbenchChrome`. | done | Preserve; no code change required. |

## Current extraction decision

The bounded safe extraction completed in this loop is `avance/salidas`: it is a coherent rich unit, it already had a canonical source in the monolith, and it did not require backend changes, connector changes, schema changes, or a mass migration of the territorial state machine.

Fuentes/source console is now extracted as a territorial-profile component with real source actions, selectable batch reconciliation, Kobo job progress polling, and a repaired `Formulario` first state that matches the canonical active-form workflow. Mutating endpoint execution was not clicked during visual QA against the audit project, but the handler path is typed, built, and wired to existing APIs.

Modelo now has a functional summary/table workbench and its canonical coverage atlas/map. Avance now has a richer `TerritorialAdvanceWorkbench` for summary, UMP table/detail, rhythm, the first interactive UMP/manzana map repair, a lazy GPS layer with selected-manzana filtering and click-to-focus response behavior, and an opt-in rich UMP map layer stack for the selected district. Iteration 18 also improves request hygiene: duplicate frontend `source` hydration during initial load was removed, and repeated resolved `warmupCache` calls now reuse the client cache. Iteration 19 changes the `avance/ump` first-render scope to `route_summary`, defers `advance_summary` hydration for `resumen`, and avoids cold `advance_summary` in the measured Mapa y UMP path. Iteration 20 restores canonical loading feedback for active pending scopes and keeps background prefetch from making the whole chrome appear blocked. Iteration 22 aligns the direct `route_summary` builder with scoped-report trimming by removing Kobo schema field/choice lists from route/advance summaries. Iteration 25 further dedupes route/advance payloads by removing duplicate `map.blocks`, duplicate `advance.*_progress`, and source-console-only UMP/enumerator summaries from the route scope, invalidating old report caches with schema `v23`; the final desktop QA passed from 1280x800 through 1920x1080 and `build:fast` passed. Iteration 28 closes the global loading/error state by replacing the target fallback error block with canonical `Alert`/retry chrome and by pausing automatic report hydration after a report error until the user clicks retry. Iteration 29 improves global phase/cache visibility in the modular rail: pilot/field badges, active source, local/snapshot rows, last sync, and report-cache status now come from backend coherence/cache metadata. Iteration 35 closes `avance/ritmo`: the frontend prioritizes canonical top-level `reports.daily`, `advance_summary` v24 now runs the canonical territorial audit before scoped trimming, ACNURCG returns 13 daily rows (`total=1283`, `validas=982`, `revision=233`), and the UI renders the rhythm chart/table with no visual issues while `response_audit` and map points stay stripped. Iteration 36 restores visible `avance/ump` street, context, zone, neighbor, and GPS-state legend behavior with clean ACNURCG QA. Full parity for `avance` still remains open because the exact original map engine, interaction depth, and measured cold backend paths are still not equivalent. Full parity for `calidad`, `consultas`, and `ocurrencias` remains blocked or mismatched until those workflows are extracted as explicit rich units with their state, handler, map, and cache contracts.

Calidad has begun with bounded geolocation, reconciliation, duration, quota, and annulment repairs. The new target no longer uses the lightweight `ter-lite-map` for `calidad/geolocalizacion`; it renders canonical `mon-territorial-validation-geo-*`, `mon-territorial-route-map-*`, `mon-territorial-map-*`, and `mon-territorial-geo-case-*` surfaces. Iteration 37 tightens that map path with drag/trackpad pan, reset, selected-UMP auto-focus, GPS point auto-focus, stable point sizing under zoom, selected-UMP GPS filtering, and a visible GPS-state legend. Iteration 38 adds active-district-first cartography loading, selected-zone and neighbor manzana context, short/full INEI zone-key normalization, and the original hierarchy where selecting the manzana card focuses the UMP while GPS points stay nested until the accordion is opened. Iteration 39 adds QA wait support and proves the post-accordion flow `Validación` -> `Abrir puntos GPS` -> `Punto 1`, preserving the distinction between focusing the manzana and opening its nested GPS list. Iteration 40 reuses the route cartography rich-layer cache for selected-district streets and context features, so the validation map shows the UMP inside its Hoja de Ruta street/context texture instead of as an isolated block cluster. Iteration 41 restores virtualized UMP groups, scroll/focus behavior for selected responses, canonical-style group risk summaries, and an explicit child rail. Iteration 42 restores district/section headings and section counts over the virtualized geolocation case rail. Iteration 43 rechecks the original monolith and publication helper, then changes the child rail from geometric manzana containment copy to the published GPS criterion: `En zona`, `Fuera de zona`, `Fuera de distrito`, `Sin cruce`, `Sin GPS`. Iteration 44 restores the original richer per-point details inside the opened child rail: GPS-only `Punto N`, date/hour, H/M/S/D + age, responsible/code fallback, district/ubigeo, UMP reference, GPS source/reclassification/precision trace, and fallback preservation of primary/effective GPS fields. Iteration 45 restores the original case-first section split for mixed UMP groups, so no-GPS rows can move to `Sin punto geográfico` while GPS rows remain in the route section and map focus still uses the base UMP. Iteration 46 compacts the in-map GPS/ruta legend with mini pills and shorter overlay labels so it fits at 1440x1000 and 1280x800 without clipping, while preserving full meanings in hover titles. Iteration 47 removes that redundant upper overlay and relies on the lower five-KPI strip as the only in-map summary, fitting all GPS states in one row. Iteration 48 re-centers the validation map on the original contextual priority: surrounding manzanas within 500 m of selected UMP/GPS anchors and readable principal street names, with street lines de-emphasized. Iteration 49 corrects the radial-context selector so surrounding manzanas do not exclude loaded manzanas from other zones; other-zone neighbors are prioritized and rendered as subtle cross-zone context. Iteration 50 keeps the compact desktop map/list layout in two columns from 1121px to 1320px and only collapses vertically below 1120px, preventing the 1280x800 map from splitting into separate bands. `calidad/reconciliacion` now renders the spatial reconciliation panel with the canonical `mon-territorial-spatial-*` classes, uses existing batch/dismiss endpoints, and restores the `mon-territorial-reconciliation-dialog` confirmation step before applying queued UMP changes. `calidad/duracion` now renders `TerritorialDurationControl` with the canonical `mon-duration-*` histogram, daily rhythm, review table, enumerator summary, and selected-response map handoff. `calidad/cuotas` now renders `TerritorialQuotaConsistencyPanel` with the canonical `mon-territorial-quota-*` commandbar, filters, replacement toggle, block cards, sex/age margins, and observed cross matrix. `calidad/anulacion` now renders `TerritorialProductionAnnulmentWorkspace` with the shared `EmptyState` and typed preview/apply/revert handlers. This is not a parity close because broader validation/reconciliation behavior and data-rich duration/reconciliation/annulment verification are not yet complete.

Consultas has begun with the bounded `TerritorialReviewCasesWorkbench` and `TerritorialOperationalAdjustmentsWorkspace`. The target no longer uses the simplified `ter-*` cards/table for `consultas`; it renders the canonical `mon-territorial-review-*` hero, metrics, filterbar, table shell, row classification pills, UUID copy control, and GPS/duration actions that deep-link into Calidad with the selected response. `subsanaciones` now renders the canonical `mon-operational-adjustments*` package workflow with note, apply, revert, reset, empty state, metrics, and applied history wired to existing APIs. This is not a parity close because the audit fixture exposes 0 review rows and 0 operational suggestions/applied adjustments, and the original UMP audit drawer is still trapped in the monolith.

Ocurrencias has begun with the bounded `TerritorialFieldOccurrencesWorkbench`. The target no longer shows placeholder occurrence content; it renders the canonical occurrence command shell, states/district/daily summaries, UMP follow-up list, selected UMP detail drawer, observation coverage cards, a searchable alert review board, Kobo asset selector, UMP filter set, upload/config/sync summary, and richer log entries with real `field_occurrences` data from the ACNURCG project. Existing occurrence APIs are wired for asset config, XLSForm generation, Kobo upload/import/deploy, field inspection, sync, state refresh, field-check display, and link copy. Iteration 31 verifies the `Observaciones` alert filter/search interaction against ACNURCG real data, so the local alerts tab is extracted. Iteration 32 verifies the `Por UMP` detail interaction against ACNURCG real data, including advance-without-occurrence notices, attention reasons, source-row facts, source records, and filter overflow checks, so the local UMP tab is extracted. Iteration 33 aligns and verifies `Estados general` against ACNURCG real data with the canonical `is-state` intent card, state meter, state stats, outcomes, daily rhythm, and district validas-without-occurrences cells, so the local states tab is extracted. This is not a full occurrence parity close because the upload-Kobo mutation has not been executed against a safe tokened fixture.

## Ocurrencias partial extraction contract

| Original area | Original symbol/block | Original source line/range | Role | Current target equivalent | Status | Extraction decision |
|---|---|---:|---|---|---|---|
| Component | `TerritorialFieldOccurrencesSection` / `TerritorialFieldOccurrencesView` | `MonitoreoPage.tsx:22917-24209` | Canonical command shell, form config, XLSForm, field inspection, sync, states, UMP, alerts, and history. | `TerritorialFieldOccurrencesWorkbench.tsx` renders a profile-owned occurrence workbench for `states`, `ump`, `alerts`, a compact Kobo asset selector, upload command, config summary, searchable alert rows, and richer log entries. | partial | Extracted visible data-backed tabs, selector, upload entry point, alert review board, and log/config state; mutation round-trip remains open. |
| Handler | Config / XLSForm / upload / inspect / sync | `MonitoreoPage.tsx:4280-4330`; `router_monitoreo.R:5316-5510` | Select/configure Kobo asset, generate the XLSForm, import/deploy to Kobo, inspect expected fields, sync occurrence responses, and update state. | `configureOccurrenceAsset`, `generateXlsform`, `uploadKobo`, `inspectFields`, and `syncOccurrences` call existing occurrence API helpers and pass state updates back to `TerritorialMonitoreoPage`; config/upload selection refreshes the current scope. | partial | Handler wiring passed type/build validation and visual QA; the external Kobo upload click was not executed in the audit project. |
| Render | `states` occurrence tab | `MonitoreoPage.tsx:23400-23640` | Effective/non-effective composition, outcome bars, daily rhythm, and district summary. | `OccurrenceStateComposition`, `OccurrenceOutcomeBars`, `OccurrenceDailyBars`, and `OccurrenceDistrictMatrix` render the canonical `is-state` intent card, state meter, state stats, outcome bars, daily bars, and district validas-without-occurrences cells. | extracted | ACNURCG visual QA `tmp/visual-qa/monitoreo-territorial-occurrences-states-canonical-iteration33/report.json` passed with no issues/overflow; manual QA `tmp/visual-qa/monitoreo-territorial-occurrences-states-canonical-iteration33-manual/manual-report.json` verified 5,112 attempts, 7 outcome rows, 7 daily rows, 7 district rows, 7 advance-missing district cells, no legacy composition block, and no page/console errors. |
| Render | `ump` occurrence tab | `MonitoreoPage.tsx:23640-24020` | UMP coverage filters, list, status grouping, selected detail, reasons, and advance cross-checks. | `OccurrenceUmpWorkspace`, `OccurrenceUmpListRow`, and `OccurrenceUmpDetail` render UMP rows, search/status/district/responsible/outcome filters, complete/incomplete/started missing counters, status badges, meters, metadata chips, detail drawer, advance-without-occurrence notices, attention reasons, route/cruce and source-row facts, and source-record rows. | extracted | ACNURCG visual QA `tmp/visual-qa/monitoreo-territorial-occurrences-ump-detail-iteration32-quick/report.json` passed with no issues/overflow; manual QA `tmp/visual-qa/monitoreo-territorial-occurrences-ump-detail-iteration32/manual-report.json` proved complete-missing and non-effective detail states with no page/console errors. |
| Render | `alerts` occurrence tab | `MonitoreoPage.tsx:24020-24209` | Reviewable occurrence observations, missing UMP, outside-route alerts, and history. | `alerts` renders top outcomes, missing/complete/incomplete coverage cards, observation/outside-route/high-no-effectivity counts, a searchable alert board, and a fuller history list with type/status/message/count/asset context. | extracted | ACNURCG visual QA `tmp/visual-qa/monitoreo-territorial-occurrences-alerts-iteration30/report.json` passed at 1440x1000 with `80 visibles de 148`; manual QA `tmp/visual-qa/monitoreo-territorial-occurrences-alerts-filter-iteration31/manual-report.json` proved `Sin reporte` filtering, `0390` search narrowing, and empty-state behavior with no page/console errors. |
| CSS | `mon-field-occurrences-*` | `monitoreo.css:20858-21400, 45393-49272` | Canonical occurrence visual language, UMP list/detail, states grid, district matrix, and alert cards. | Target reuses canonical class families, adds scoped `mon-field-occurrences-alert-*` selectors for the alert review board, and keeps the territorial-profile UMP row sizing override in `territorialProfile.css`. | partial | The alert board selectors passed ACNURCG visual QA without overflow; the UMP override still protects real data row height without changing backend behavior. |

## Calidad/geolocalizacion partial extraction contract

| Original area | Original symbol/block | Original source line/range | Role | Current target equivalent | Status | Extraction decision |
|---|---|---:|---|---|---|---|
| Component | `TerritorialValidationGeoWorkbench` | `MonitoreoPage.tsx:18210-18540` | Canonical geolocation workbench, reconciliation entry, GPS map, and case list. | `TerritorialValidationGeoWorkbench.tsx` renders the target geolocation tab with the same canonical class families and data sources. | partial | Extracted as scaffold; not parity-complete. |
| Component | `TerritorialValidationGeoRouteMap` | `MonitoreoPage.tsx:18749-19228` | Hojas de Ruta map with selected route blocks, zones, GPS points, controls, and footer metrics. | Target loads active-district block/zone cartography, projects selected UMP geometry, renders selected-zone and neighbor manzana context, renders selected-district street/context features, renders selected-UMP Kobo GPS points, supports drag/trackpad pan, zoom/reset controls, UMP and GPS auto-focus, stable point sizing, caption, visible GPS-state legend, and footer metrics. | partial | Iteration 40 closes the visible street/context layer gap with ACNURCG QA showing `2 zonas · 220 vias · 120 contexto`; original virtualized case behavior remains open. |
| Component | `TerritorialGeoCaseList` | `MonitoreoPage.tsx:19330-19530`; `api/R/monitoreo_engine.R:26428-26485` | Group cases by UMP/district, expand groups, focus selected GPS point, and classify GPS by territorial zone/district status. | Target groups cases by UMP, keeps the selected manzana card separate from the nested GPS accordion, focuses a selected UMP without opening points, focuses GPS only from an opened point row, virtualizes UMP groups, scrolls/focuses selected responses, renders canonical-style district/section headings, shows UMP-child zone summaries using `En zona UMP`, `Fuera zona`, and `Fuera distrito` instead of treating `Dentro/Fuera manzana` as the primary containment criterion, restores richer original point-row summaries with GPS-only `Punto N`, submission stamp, sex/age, responsible/code fallback, district/ubigeo, UMP reference, and GPS source/reclassification/precision trace, and splits mixed GPS/no-GPS UMPs by case-first section before rendering visual groups. | partial | Iteration 43 proves the post-accordion point-click flow with corrected zone/district sidebar semantics in `tmp/visual-qa/monitoreo-territorial-calidad-geo-zone-criteria-iteration43/report.json`; Iteration 44 proves the richer row-detail flow in `tmp/visual-qa/monitoreo-territorial-calidad-geo-rich-row-detail-iteration44/report.json`; Iteration 45 proves the split-safe accordion flow in `tmp/visual-qa/monitoreo-territorial-calidad-geo-mixed-gps-split-iteration45/report.json`. Broader validation/reconciliation parity remains open. |
| Component | `TerritorialSpatialReconciliationPanel` | `MonitoreoPage.tsx:18550-18735` | Render spatial patterns/candidates, evidence, impact preview, map focus, stage and dismiss controls. | `TerritorialSpatialReconciliationWorkbench` renders pattern/candidate cards, evidence chips, impact preview, response-id map focus handoff, local queue, confirmation dialog, batch apply, and dismiss controls. | partial | Candidate-rich visual/state verification remains open. |
| Component | `TerritorialDurationControlView` | `MonitoreoPage.tsx:20426-20855` | Render duration overview, histogram, daily rhythm, review rows, selected case, map handoff, and enumerator summary. | `TerritorialDurationControl.tsx` renders the canonical `mon-duration-*` workbench and preserves the selected-response handoff into geolocation. | partial | Empty-state visual QA passed; outlier-rich review rows and map handoff still need fixture verification. |
| Component | `TerritorialQuotaConsistencyPanel` | `MonitoreoPage.tsx:17483-17769` | Render quota commandbar, search/filter/replacement controls, quota cards, sex/age margins, and observed cross matrix. | `TerritorialQuotaConsistencyPanel.tsx` renders the canonical `mon-territorial-quota-*` workbench in the territorial profile. | extracted | Visual QA passed against the audit fixture with 8 quota blocks. |
| Component | `TerritorialProductionAnnulmentWorkspace` | `MonitoreoPage.tsx:17246-17478` | Render responsible selector, required reason, preview/apply annulment controls, impact table, and history/revert controls. | `TerritorialProductionAnnulmentWorkspace.tsx` renders the same workflow and uses existing annulment APIs from the territorial profile. | partial | Empty-state visual QA passed; responsible-rich impact/history behavior needs fixture verification. |
| Handler | Spatial reconciliation staging/dismissal | `MonitoreoPage.tsx:18317-18445` | Stage/dismiss/apply spatial UMP reconciliation from GPS suspicion cases. | Target stages candidates/patterns locally, focuses the candidate response in the map tab, confirms with `mon-territorial-reconciliation-dialog`, applies via `apiMonitoreoTerritorialReconciliationBatch`, and dismisses candidates/patterns through existing spatial endpoints. | partial | Endpoint wiring and focus handoff exist; candidate-rich failure-state UX still needs verification. |
| CSS | Validation geo classes | `monitoreo.css:9117-10845, 14752-15235` | Canonical visual language for GPS map and case list. | Target reuses these classes directly. | partial | Visual language restored, but behavior parity still open. |
| CSS | Quota consistency classes | `monitoreo.css:16705-17408` | Canonical quota commandbar, cards, margins, and observed matrix visual language. | `TerritorialQuotaConsistencyPanel.tsx` reuses `mon-territorial-quota-*` classes directly. | extracted | No `ter-*` replacement chrome introduced for `cuotas`. |
| CSS | Production annulment classes | `monitoreo.css:54117-54444` | Canonical production annulment visual language for controls, impact, and history. | `TerritorialProductionAnnulmentWorkspace.tsx` reuses `mon-production-annulments*` classes when responsible data exists and the shared `EmptyState` when it does not. | partial | No `ter-*` replacement chrome introduced for `anulacion`. |

## Fuentes/source console extraction contract

| Original area | Original symbol/block | Original source line/range | Role | Current target equivalent | Status | Extraction decision |
|---|---|---:|---|---|---|---|
| Constants | `TerritorialSourceTab` | `MonitoreoPage.tsx:7176` | Defines source subtabs `form`, `filter`, `roster`, `reconciliation`, `history`. | `TerritorialSourceConsole.tsx` keeps the same five subtab keys and reacts to the canonical rail event. | extracted | Preserve exact subtab contract. |
| State | asset list/search/filter/pending asset | `MonitoreoPage.tsx:10870-10920` | Local Kobo asset picker state. | Local state in `TerritorialSourceConsole`. | extracted | Uses `apiMonitoreoKoboAssets`. |
| State | pending reconciliation batch state | `MonitoreoPage.tsx:10930-11025` | Batch staging/apply/confirm for code and UMP changes. | `TerritorialSourceConsole` keeps selected recommendation IDs, batch applying state, and result messaging. | extracted | Applies the existing batch endpoint without schema/backend changes. |
| Handler | `loadAssets()` | `MonitoreoPage.tsx:11027` | Load Kobo assets for selected base URL/profile. | `loadAssets` in `TerritorialSourceConsole`. | extracted | Existing API reused. |
| Handler | `useTerritorialKoboAsset()` | `MonitoreoPage.tsx:3831` | Apply a Kobo asset to the current territorial phase. | `applyAsset` in `TerritorialSourceConsole`. | extracted | Uses `apiMonitoreoTerritorialSource`. |
| Handler | `apiMonitoreoTerritorialInspectKobo()` call path | `MonitoreoPage.tsx:3814, 4883` | Inspect schema/coherence for active Kobo asset. | `inspectKobo` in `TerritorialSourceConsole`. | extracted | Existing endpoint reused. |
| Handler | `patchTerritorialConfig()` for mappings/filter | `MonitoreoPage.tsx:3872` | Persist variable mappings and filter values. | `saveMapping` in `TerritorialSourceConsole`. | extracted | Uses `apiMonitoreoTerritorialConfig`. |
| Handler | route-sheet source connect/sync | `MonitoreoPage.tsx:11574-12190` | Save and sync Hojas de Ruta source. | `saveRouteSheet` and `syncRouteSheet` in `TerritorialSourceConsole`. | extracted | Uses `apiMonitoreoSheetsSource` and `apiMonitoreoSheetsSync`. |
| Handler | roster upload/downloads | `MonitoreoPage.tsx:3915-3998, 4172-4186` | Upload roster and download template/codes. | `uploadRoster`, `downloadRosterTemplate`, `downloadRosterCodes`. | extracted | Existing endpoints reused. |
| Render | `form` tab | `MonitoreoPage.tsx:11080-11390` | Active form, asset picker, inspection/sync actions. | `data-source-tab="form"` in `TerritorialSourceConsole` renders the phase source strip, Hojas de Ruta source controls, selected Kobo form summary, readiness metrics, and inspect/sync actions; the asset picker remains behind `Cambiar formulario`. | extracted | Iteration 34 visual QA `tmp/visual-qa/monitoreo-territorial-source-form-user-diff-repair/report.json` passed after the user-reported comparator mismatch was reproduced in `tmp/visual-qa/monitoreo-territorial-user-reported-ui-diff/quick-monitoreo-comparar-territorial-3000x1100-auto.png`. |
| Render | `filter` tab | `MonitoreoPage.tsx:11396-11418`, `12205+` | Operational variables, valid-response filter, district coverage. | `data-source-tab="filter"` in `TerritorialSourceConsole`. | extracted | Click QA artifact: `tmp/visual-qa/monitoreo-territorial-source-filter`. |
| Render | `roster` tab | `MonitoreoPage.tsx:11420-11442`, `14000+` | Enumerator roster, upload, template and code downloads. | `data-source-tab="roster"` in `TerritorialSourceConsole`. | extracted | Click QA artifact: `tmp/visual-qa/monitoreo-territorial-source-roster`. |
| Render | `reconciliation` tab | `MonitoreoPage.tsx:11444-11475`, `11780+` | Code/UMP reconciliation and route-sheet controls. | `data-source-tab="reconciliation"` shows real route-sheet state, selectable code/UMP recommendations, and batch apply controls. | extracted | Click QA artifact: `tmp/visual-qa/monitoreo-territorial-source-reconciliation-batch`. |
| Render | `history` tab | `MonitoreoPage.tsx:11477-11510` | Inspection/sync history. | `data-source-tab="history"` in `TerritorialSourceConsole`. | extracted | Click QA artifact: `tmp/visual-qa/monitoreo-territorial-source-history`. |
| CSS | `mon-territorial-source-*` | `monitoreo.css:10851-12350` | Canonical source console visual language. | Reused directly by `TerritorialSourceConsole`. | extracted | No replacement chrome namespace introduced. |

## Modelo/route workbench extraction contract

| Original area | Original symbol/block | Original source line/range | Role | Current target equivalent | Status | Extraction decision |
|---|---|---:|---|---|---|---|
| Type | `TerritorialRouteWorkbenchTab` | `MonitoreoPage.tsx:14951` | Defines Modelo local tabs `resumen` and `tabla`. | `TerritorialModelWorkbench.tsx` accepts `activeLocalTab` and switches `resumen`/`tabla`. | extracted | Preserve exact subtab contract. |
| Handler | `onSyncKobo` / `apiMonitoreoSync()` route command | `MonitoreoPage.tsx:15080` | Sync active Kobo source from Modelo command. | `TerritorialModelWorkbench` calls `apiMonitoreoSync()` for the phase Kobo source and polls `apiJobStatus()`. | extracted | Mutating click not executed in QA; handler is type/build validated. |
| State | selected/inspected block keys | `MonitoreoPage.tsx:15000-15047` | Keep selected titular and inspected replacement/context. | Local `selectedBlockKey` and `inspectedBlockKey` in `TerritorialModelWorkbench`. | extracted | Keeps table/context interaction independent from the monolith. |
| Helper | route block ordering and labels | `MonitoreoPage.tsx:16731-16888` | Order titulares/replacements and render UMP/range labels. | Local helpers in `TerritorialModelWorkbench` mirror comparator, range, UMP and replacement labels. | extracted | Duplicated temporarily to avoid importing `MonitoreoPage`. |
| Render | route command | `MonitoreoPage.tsx:15078-15105` | Active source, phase facts, and sync command. | `TerritorialModelWorkbench` route command with `mon-territorial-route-command`. | extracted | Uses existing route classes. |
| Render | route sheet strip | `MonitoreoPage.tsx:21919-22000` | Show hoja de ruta assignments, warnings and reconciliation state. | `RouteSheetStrip` inside `TerritorialModelWorkbench`. | extracted | Uses existing route-sheet classes. |
| Render | `TerritorialRouteSummary` | `MonitoreoPage.tsx:15148-15250` | Coverage summary for UMPs, replacements, districts, responsibles and phase goal. | `TerritorialRouteCoverageAtlas` restores the canonical hero, KPIs, selected-block map, sex/age rail, and district coverage table. | extracted | Click QA artifact: `tmp/visual-qa/monitoreo-territorial-modelo-cobertura-atlas`. |
| Render | `TerritorialRouteBlockTable` | `MonitoreoPage.tsx:15646-15818` | Filterable titulares table with replacement rows. | `RouteBlockTable` with district/search filters, accordion, replacement rows and selected state. | extracted | Click QA artifacts: `tmp/visual-qa/monitoreo-territorial-modelo-tabla-fixed2` and compact. |
| Render | `TerritorialRouteBlockContext` | `MonitoreoPage.tsx:16200-16545` | Selected UMP details, assignments, quotas and operational facts. | `RouteBlockContext` shows selected UMP facts, assignments and quota marginal snapshot. | partial | Response-owner and full point/map context remain in future map/validation work. |
| Render | `TerritorialRouteCoverageMap` / `TerritorialRouteAdaptiveMap` | `MonitoreoPage.tsx:15270-15630, 15820-16220` | Static coverage map and adaptive selected block cartography. | `TerritorialRouteCoverageAtlas` extracts the rendered static coverage map used by `modelo/Cobertura`; `TerritorialRouteAdaptiveMap` remains mapped as a historical helper not mounted by the current original `TerritorialRouteView`. | extracted | Route coverage map loads local Hojas de Ruta block/zone cartography and renders selected titulares/replacements with the canonical classes. |
| CSS | `mon-territorial-route-*` | `monitoreo.css:13123-14495, 38152-39040` | Canonical Modelo visual language. | Reused by `TerritorialModelWorkbench`; `territorialProfile.css` only fixes height/scroll for independent page. | extracted | No `ter-*` replacement chrome. |
