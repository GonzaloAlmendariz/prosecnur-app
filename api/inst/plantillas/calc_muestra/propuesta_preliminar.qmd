---
title: "`r params$titulo`"
subtitle: "Propuesta metodológica preliminar — estimación inicial"
date: today
lang: es
format:
  html:
    toc: true
    toc-depth: 3
    toc-location: left
    df-print: paged
    theme: cosmo
    embed-resources: true
execute:
  echo: false
  warning: false
  message: false
params:
  titulo: "Estudio sin título"
  bundle_path: ""
---

```{r setup}
suppressPackageStartupMessages({
  library(tibble)
  library(dplyr)
  library(knitr)
  library(kableExtra)
})

stopifnot(nzchar(params$bundle_path), file.exists(params$bundle_path))
bundle  <- readRDS(params$bundle_path)
estudio <- bundle$estudio

pulsoazul <- "#1F3864"

tabla_pulso <- function(df, caption = NULL) {
  kable(df, caption = caption, format.args = list(big.mark = ",")) |>
    kable_styling(bootstrap_options = c("striped", "hover", "condensed"),
                  full_width = FALSE, position = "left") |>
    row_spec(0, bold = TRUE, color = "white", background = pulsoazul)
}

`%||%` <- function(x, y) if (is.null(x) || length(x) == 0L) y else x

fmt_int <- function(x) if (is.null(x) || is.na(x)) "—" else format(x, big.mark = ",", scientific = FALSE)
fmt_pct <- function(x, dec = 1) if (is.null(x) || is.na(x)) "—" else sprintf(paste0("%.", dec, "f%%"), x * 100)
```

## Resumen del estudio

- **Título:** `r estudio$titulo`
- **Macro-familia:** `r estudio$macro_familia`
- **Fase:** estimación preliminar (propuesta inicial)
- **Cliente:** `r estudio$contexto$cliente %||% "—"`
`r if (isTRUE(estudio$modo_sensible)) "- **Consideraciones éticas:** estudio requiere protocolos de investigación con población especial." else ""`

::: {.callout-note}
Este reporte corresponde a una **estimación preliminar**: los universos pueden no estar
todavía validados y las metas son indicativas. El diseño debe refinarse en la fase
`diseno_validado` con bases recibidas y limpiadas.
:::

## Componentes propuestos

```{r componentes_tabla}
filas <- lapply(estudio$componentes, function(c) {
  res <- c$resultado
  tibble(
    Actor             = c$actor,
    Tecnica           = c$tecnica,
    Origen            = c$origen_tamano,
    Marco_validado    = fmt_int(c$marco$marco_validado),
    n_objetivo        = fmt_int(res$n_objetivo %||% NA),
    n_operativo       = fmt_int(res$n_operativo %||% NA),
    Precision         = if (!is.null(res$precision_alcanzada) && !is.na(res$precision_alcanzada))
                          fmt_pct(res$precision_alcanzada) else "-"
  )
})
tabla_pulso(do.call(rbind, filas), caption = "Componentes del estudio")
```

## Detalle por componente

```{r detalle_componentes, results="asis"}
for (c in estudio$componentes) {
  cat(sprintf("### %s — %s\n\n", c$actor, c$tecnica))
  cat(sprintf("- **Naturaleza inferencial:** %s\n", c$naturaleza))
  cat(sprintf("- **Origen del tamaño:** %s\n", c$origen_tamano))
  cat(sprintf("- **Nivel de respaldo:** %s\n", c$nivel_respaldo))
  cat(sprintf("- **Marco:** bruto=%s · validado=%s · contactable=%s · estado=%s\n",
              fmt_int(c$marco$universo_bruto),
              fmt_int(c$marco$marco_validado),
              fmt_int(c$marco$marco_contactable),
              c$marco$estado))
  if (!is.null(c$resultado)) {
    r <- c$resultado
    cat(sprintf("- **n objetivo:** %s\n", fmt_int(r$n_objetivo)))
    cat(sprintf("- **n operativo:** %s\n", fmt_int(r$n_operativo)))
    if (!is.null(r$advertencia)) {
      cat(sprintf("\n> ⚠ %s\n", r$advertencia))
    }
  }
  cat("\n")
}
```

## Cuotas operativas calculadas

```{r cuotas_matriz, results="asis"}
for (c in estudio$componentes) {
  res <- c$resultado
  if (is.null(res$cuotas_matriz) || length(res$cuotas_matriz) == 0L) next
  cat(sprintf("### %s\n\n", c$actor))
  df <- do.call(rbind, lapply(res$cuotas_matriz, function(x) {
    data.frame(
      Territorio = x$territorio, Servicio = x$servicio,
      Marco = x$N, Cuota = x$n,
      Regla = x$regla %||% "",
      stringsAsFactors = FALSE, check.names = FALSE
    )
  }))
  print(tabla_pulso(df, caption = "Matriz de cuotas calculada desde el marco operativo"))
  cat("\n\n")
}
```

## Lineamientos metodológicos

1. Cada actor puede tener un diseño distinto: el estudio es contenedor de componentes.
2. Marco y unidad se separan: `unidad_seleccion` ≠ `unidad_analisis`.
3. Solo diseños probabilísticos con marco completo y probabilidad conocida producen margen de error formal.
4. Los demás diseños se reportan como cobertura operacional, representatividad teórica o validez interna.

## Cómo se estima la efectividad esperada de cada facultad

```{r efectividad_metodo, results="asis"}
tf <- bundle$tasas_facultad
cat("El número de aulas de cada facultad sale de una sola fórmula:\n\n")
cat("$$\\text{aulas} = \\left\\lceil \\frac{\\text{cuota}}{\\text{alumnos por curso-horario} \\times \\tau} \\right\\rceil$$\n\n")
cat(paste0(
  "La cuota es la parte de la sobremuestra operativa que corresponde a la facultad ",
  "por afijación, y los alumnos por curso-horario son el estadístico declarado sobre ",
  "sus elegibles. El tercer término, la efectividad esperada $\\tau$, es el que exige ",
  "sustento: no se observa, se estima.\n\n"))
cat(paste0(
  "**Procedimiento: estandarización directa por tamaño de conglomerado.** El aula es ",
  "la unidad de conglomerado, y su tamaño es el factor que más pesa en cuánto rinde ",
  "(un aula chica rinde una fracción mayor de sus elegibles que una grande). ",
  "La estimación se hace en dos capas:\n\n"))
cat(paste0(
  "1. **Función de efectividad según tamaño**, estimada sobre el estudio anterior ",
  "completo, con las aulas de todas las facultades juntas. Es *común a todas*.\n",
  "2. **Composición por tamaño** de cada facultad, tomada del marco vigente: qué ",
  "proporción de sus elegibles está en aulas de cada tramo. Es *propia de cada una*.\n\n"))
cat(paste0(
  "El producto de ambas da la efectividad esperada de la facultad bajo el supuesto de ",
  "que se comporta como predice el tamaño de sus aulas. Cuando el estudio anterior ",
  "aporta suficientes aulas de esa facultad se añade una tercera capa:\n\n"))
cat(paste0(
  "3. **Razón observado/esperado**, el cociente entre lo que la facultad rindió de ",
  "hecho y lo que su composición predecía. Corrige lo que es propio de la facultad ",
  "más allá de sus tamaños. Se estima sólo con **12 aulas aplicadas o más**; por ",
  "debajo se fija en 1 (valor neutro).\n\n"))
cat(paste0(
  "**Por qué la función de tamaño es común y no por facultad.** Estimarla por ",
  "facultad exige poblar una celda por cada combinación de facultad y tramo de ",
  "tamaño. Sobre el estudio anterior, la mayoría de esas celdas quedan vacías o con ",
  "un puñado de aulas, mientras la estimación común reparte las mismas ",
  "observaciones entre unos pocos tramos. Se pone en común lo que necesita muchos ",
  "datos (el efecto del tamaño, sin razón teórica para operar distinto según la ",
  "facultad) y se deja propio lo que se puede estimar con pocos (un solo parámetro ",
  "por facultad).\n\n"))
cat(paste0(
  "**Sobre el umbral de 12 aulas.** Se evaluó sustituir el corte por un encogimiento ",
  "parcial, que acerca la razón observado/esperado a 1 cuanto menor es el respaldo en ",
  "vez de saltar entre dos valores. La descomposición de la varianza mostró que una ",
  "parte considerable de la dispersión observada entre facultades es error de ",
  "estimación y no diferencia verdadera, lo que respalda encoger. No se adoptó porque ",
  "la estimación del error se vuelve poco fiable justamente donde más haría falta: ",
  "con tres o cuatro aulas, un grupo que casualmente rindió parejo produce una ",
  "varianza pequeña que el método lee como precisión y pondera de más. El corte duro ",
  "es menos eficiente y más conservador, y esa es la decisión vigente.\n\n"))
cat(paste0(
  "**Sin estudio anterior.** Si el proyecto no declara un histórico, no hay función ",
  "de tamaño ni razón que estimar: se aplica una tasa de referencia única, igual para ",
  "todas las facultades, y el documento lo declara como tal.\n\n"))
```

```{r efectividad_tabla, results="asis"}
tf <- bundle$tasas_facultad
if (!is.null(tf) && length(tf)) {
  df <- do.call(rbind, lapply(tf, function(t) {
    mix <- t$rendimiento_mix; f <- t$factor_residual
    data.frame(
      Facultad = t$facultad,
      `Aulas en el marco` = t$n_aulas,
      `Composición por tamaño` = if (is.null(mix) || !is.finite(mix)) "—" else sprintf("%.3f", mix),
      `Razón O/E` = if (is.null(f) || !is.finite(f) || abs(f - 1) < 1e-9) "1 (neutro)" else sprintf("%.3f", f),
      `Aulas 2025` = if (is.null(t$facultad_k) || is.na(t$facultad_k)) "menos de 12" else as.character(t$facultad_k),
      `Efectividad esperada` = sprintf("%.1f%%", 100 * t$tasa),
      stringsAsFactors = FALSE, check.names = FALSE
    )
  }))
  print(tabla_pulso(df, caption = paste(
    "Efectividad esperada por facultad. «Composición por tamaño» es la efectividad",
    "que predice la mezcla de tamaños de sus aulas; «Razón O/E» corrige lo que la",
    "facultad rindió por encima o por debajo de esa predicción en el estudio anterior.")))
  cat("\n\n")
} else {
  cat("*(El desglose por facultad no está disponible para este estudio: no hay marco de aulas construido.)*\n\n")
}
```

## Sustento metodológico

```{r sustento_metodologico}
fuentes <- bundle$metodologia
if (is.null(fuentes) || !nrow(as.data.frame(fuentes))) {
  cat("_El motor no publicó el bloque de fuentes metodológicas para esta corrida._\n")
} else {
  fuentes <- as.data.frame(fuentes, stringsAsFactors = FALSE)
  limpio <- function(x) { x <- as.character(x); x[is.na(x) | !nzchar(trimws(x))] <- "—"; x }
  # Sólo lo que un lector externo necesita: la decisión, la regla que aplica la
  # app y de dónde sale. Los URLs quedan en el anexo, no rompen la tabla aquí.
  tabla <- data.frame(
    `Decisión metodológica` = limpio(fuentes$decision_metodologica),
    `Regla aplicada`        = limpio(fuentes$regla_app),
    `Referencia oficial`    = limpio(fuentes$official_reference),
    `Referencia académica`  = limpio(fuentes$academic_reference),
    check.names = FALSE, stringsAsFactors = FALSE
  )
  tabla_pulso(tabla, caption = "Decisiones de diseño y su respaldo")
}
```

```{r sustento_implementacion}
if (!is.null(fuentes) && nrow(as.data.frame(fuentes))) {
  f2 <- as.data.frame(fuentes, stringsAsFactors = FALSE)
  impl <- as.character(f2$implementation_reference)
  impl <- unique(trimws(impl[!is.na(impl) & nzchar(trimws(impl))]))
  if (length(impl)) {
    cat("**Implementación.** El sorteo se ejecuta con ",
        paste(sprintf("`%s`", impl), collapse = ", "),
        ". La versión usada y cualquier fallback quedan registrados en la auditoría de la corrida.\n", sep = "")
  }
}
```

::: {.callout-note}
La selección es probabilística: toda unidad del marco tiene una probabilidad de
inclusión conocida y mayor que cero, y esa probabilidad es la que pondera los
resultados. Cada decisión de diseño está anclada en estándares de operativos
internacionales de evaluación educativa y en la literatura de muestreo balanceado.
:::

## Próximos pasos

1. Validar marcos por actor: limpieza, deduplicación, validación de rol único.
2. Confirmar canal de campo y tasa de respuesta esperada por componente.
3. Avanzar a "diseño validado" cuando las bases estén listas para definir cuotas finales.
