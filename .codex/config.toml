# Configuración mínima del agentic OS de Prosecnur para Codex.
# Los agentes especializados se definen en .codex/agents/.

[features]
multi_agent = true

[agents]
max_threads = 4
max_depth = 1
interrupt_message = true
